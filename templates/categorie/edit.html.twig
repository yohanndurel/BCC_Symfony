{% extends 'base.html.twig' %}

{% block title %}Modifier la catégorie "{{ categorie.nom }}"{% endblock %}

{% block body %}
    <h1>Modifier la catégorie "{{ categorie.nom }}"</h1>

    {{ include('categorie/_form.html.twig', {'button_label': 'Appliquer'}) }}

    <a href="{{ path('categorie_index') }}">back to list</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
