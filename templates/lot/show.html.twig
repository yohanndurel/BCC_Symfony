{% extends 'base.html.twig' %}

{% block title %}Lot{% endblock %}

{% block body %}
    <h1>Lot</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lot.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ lot.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ lot.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('encherir_new') }}">Encherir</a><br>

    <a href="{{ path('lot_index') }}">Retour</a>

    <a href="{{ path('lot_edit', {'id': lot.id}) }}">Modifier</a>

    {{ include('lot/_delete_form.html.twig') }}
{% endblock %}
