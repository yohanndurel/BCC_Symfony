{% extends 'base.html.twig' %}

{% block title %}Modifier le produit "{{ produit.nom }}"{% endblock %}

{% block body %}
    <h1>Modifier le produit "{{ produit.nom }}"</h1>

    {{ include('produit/_form.html.twig', {'button_label': 'Appliquer'}) }}

    <a href="{{ path('produit_index') }}">Retour</a>

    {{ include('produit/_delete_form.html.twig') }}
{% endblock %}
