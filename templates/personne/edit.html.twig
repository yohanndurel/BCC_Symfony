{% extends 'base.html.twig' %}

{% block title %}Modifier la personne "{{ personne.nom }}"{% endblock %}

{% block body %}
    <h1>Modifier la personne "{{ personne.nom }}"</h1>

    {{ include('personne/_form.html.twig', {'button_label': 'Appliquer'}) }}

    <a href="{{ path('personne_index') }}">Retour</a>

    {{ include('personne/_delete_form.html.twig') }}
{% endblock %}
