<form method="post" action="{{ path('ordre_achat_delete', {'id': ordre_achat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ordre_achat.id) }}">
    <button class="btn">Delete</button>
</form>
