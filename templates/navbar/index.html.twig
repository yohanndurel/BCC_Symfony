<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path("home") }}"><i class="fad fa-joint fa-2x"></i></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <a class="dropdown-item" href="{{ path("enchere_index") }}">Ventes au Enchères</a>
            <a class="dropdown-item" href="{{ path("lot_index") }}">Lots</a>
            <a class="dropdown-item" href="{{ path("produit_index") }}">Parcourir les Produits</a>
{#            <a class="dropdown-item" href="#"></a>#}
        </ul>
        {% if app.user %}
            <a type="button" class="btn btn-outline-info" href="{{ path("app_logout") }}">Déconnexion</a>
        {% else %}
            <a type="button" class="btn btn-outline-info" href="{{ path("app_login") }}">Connexion</a>
            <a type="button" class="btn btn-outline-info" href="{{ path("app_register") }}">Inscription</a>
        {% endif %}
    </div>
</nav>
