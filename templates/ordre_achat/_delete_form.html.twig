<form method="post" action="{{ path('ordre_achat_delete', {'id': ordre_achat.id}) }}" onsubmit="return confirm('Êtes-vous sur de vouloir supprimer cette objet ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ordre_achat.id) }}">
    <button class="btn">Supprimer</button>
</form>
