{% extends 'base.html.twig' %}

{% block title %}Modifier le lot "{{ lot.nom }}"{% endblock %}

{% block body %}
    <h1>Modifier le lot "{{ lot.nom }}"</h1>

    {{ include('lot/_form.html.twig', {'button_label': 'Appliquer'}) }}

    <a href="{{ path('lot_index') }}">Retour</a>

    {{ include('lot/_delete_form.html.twig') }}
{% endblock %}
